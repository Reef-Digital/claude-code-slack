{
  "name": "claude-code-slack",
  "version": "0.9.0",
  "description": "Slack channel plugin for Claude Code — interact with Claude Code agents via Slack",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "build": "bun build server.ts --target=bun --outfile=dist/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slack/bolt": "^4.1.0"
  }
}
